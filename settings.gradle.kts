pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "FoodFor-KMM"
include(":androidApp")
include(":shared")
include(":desktop")